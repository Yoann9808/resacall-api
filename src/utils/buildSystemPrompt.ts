// Build dynamic system prompt per restaurant:
// Injects: name, timezone, detected_language, services (hours/max_covers/booking_deadline),
// today's date (to resolve "demain", "vendredi"), agent_instructions, confirmation_message

export {};
