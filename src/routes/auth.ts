import type { FastifyInstance } from 'fastify';

export async function authRoutes(_fastify: FastifyInstance): Promise<void> {
  // POST /auth/register
  // POST /auth/login
  // POST /auth/refresh
  // POST /auth/logout
  // POST /auth/forgot-password
  // POST /auth/reset-password
  // GET  /auth/me
}
