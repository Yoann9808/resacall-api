import type { FastifyInstance } from 'fastify';

export async function restaurantsRoutes(_fastify: FastifyInstance): Promise<void> {
  // GET  /restaurants/:id
  // PUT  /restaurants/:id
  // PUT  /restaurants/:id/settings
  // PUT  /restaurants/:id/availability
  // PUT  /restaurants/:id/exceptions
  // PUT  /restaurants/:id/notifications
}
