import type { FastifyInstance } from 'fastify';

export async function statsRoutes(_fastify: FastifyInstance): Promise<void> {
  // GET /restaurants/:id/stats/overview
  // GET /restaurants/:id/stats/calls
  // GET /restaurants/:id/stats/reservations
}
