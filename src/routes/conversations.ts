import type { FastifyInstance } from 'fastify';

export async function conversationsRoutes(_fastify: FastifyInstance): Promise<void> {
  // GET  /restaurants/:id/conversations
  // POST /restaurants/:id/conversations/:conversationId/takeover
}
