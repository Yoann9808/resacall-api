import type { FastifyInstance } from 'fastify';

export async function reservationsRoutes(_fastify: FastifyInstance): Promise<void> {
  // GET   /restaurants/:id/reservations
  // POST  /restaurants/:id/reservations
  // PATCH /restaurants/:id/reservations/:reservationId
  // PUT   /restaurants/:id/reservations/:reservationId
}
